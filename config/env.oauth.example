# GitHub OAuth Configuration for Qalia UI
# Copy this file to .env and configure with your GitHub OAuth App credentials

# GitHub OAuth App Settings
# Create a GitHub OAuth App at: https://github.com/settings/applications/new
GITHUB_OAUTH_CLIENT_ID=your_oauth_client_id_here
GITHUB_OAUTH_CLIENT_SECRET=your_oauth_client_secret_here
GITHUB_OAUTH_REDIRECT_URI=http://localhost:8000/api/auth/callback

# Existing GitHub App Settings (for automated operations)
GITHUB_APP_ID=your_github_app_id_here
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nyour_private_key_here\n-----END RSA PRIVATE KEY-----"

# Session Configuration
SESSION_DURATION_HOURS=24

# Development/Production Settings
ENVIRONMENT=development
DEBUG=true

# Security Settings (set these in production)
SECURE_COOKIES=false
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (when implemented)
# DATABASE_URL=sqlite:///./qalia.db 